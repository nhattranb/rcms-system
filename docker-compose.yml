version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: rcms_postgres
    restart: always
    environment:
      POSTGRES_USER: nhat
      POSTGRES_PASSWORD: thdcyber26
      POSTGRES_DB: rcms_db
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
